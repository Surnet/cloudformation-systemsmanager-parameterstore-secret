{
    "name": "surnet-parameterstore-secret",
    "version": "0.1.0",
    "description": "AWS custom resource provider named Surnet::ParameterStore::Secret.",
    "private": true,
    "main": "dist/handlers.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc",
        "prepack": "npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.6",
        "class-transformer": "0.5.1",
        "esbuild": "^0.25.0"
    },
    "devDependencies": {
        "@types/node": "^20.17.23",
        "typescript": "^5.8.2"
    }
}
